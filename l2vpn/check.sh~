
node1=clab-evpn-l2vpn-N1
node2=clab-evpn-l2vpn-N2

# N2 IF up
docker exec -it $node2 ip link set dev eth1 up

sleep 20

# Check status
echo "N1 show ip route"
docker exec -it $node1 vtysh -c "show ip route"

echo "N1 show bgp"
docker exec -it $node1 vtysh -c "show bgp summary"

echo "N1 show bgp l2vpn evpn"
docker exec -it $node1 vtysh -c "show bgp summary"

